# ============================================================================
# DEPRECATED: Use pyproject.toml instead
# ============================================================================
#
# This requirements.txt is kept for backwards compatibility.
# For new installations, use UV with pyproject.toml:
#
#   # Create virtual environment with UV
#   uv venv --python 3.12
#   source .venv/bin/activate
#
#   # Install with JAX CUDA 13 support (NVIDIA GPU)
#   uv pip install -e ".[full]"
#
#   # Or for CPU-only development
#   uv pip install -e ".[full-cpu]"
#
# See pyproject.toml for the full dependency specification.
# ============================================================================

# Core dependencies (pinned for reproducibility)
-e .[full]
